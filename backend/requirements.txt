Flask==3.0.3
Flask-Cors==4.0.1
Flask-SQLAlchemy==3.1.1
Flask-JWT-Extended==4.6.0
PyMySQL==1.1.1
cryptography==42.0.8
python-dotenv==1.0.1
ultralytics==8.4.69
huggingface_hub==0.36.2
# transformers 4.51.3：VibeVoice 需要；已验证仍保留 QA/翻译/摘要/TTS 等全部 pipeline。
transformers==4.51.3
timm==1.0.27
sentencepiece==0.2.1
imageio==2.37.3
imageio-ffmpeg==0.6.0
modelscope==1.20.1
# 语音识别(SenseVoice)：funasr 框架 + torchaudio 加载音频。
# 注意 torch 全家桶须对齐：torch/torchaudio 2.11.0、torchvision 0.26.0（CPU 版，pytorch cpu 源）。
torch==2.11.0
torchvision==0.26.0
torchaudio==2.11.0
funasr==1.3.10
# CosyVoice2 文本转语音/零样本克隆：依赖 onnxruntime(cpu) + 下列 + 官方代码。
# 官方推理代码非 pip 包：git clone --recursive https://github.com/FunAudioLLM/CosyVoice
#   到 third_party/CosyVoice（含 third_party/Matcha-TTS），运行时由 inference._ensure_cosyvoice_path 注入 sys.path。
# 其重依赖(lightning/conformer/diffusers/hyperpyyaml/onnxruntime 等)随 CosyVoice 安装；此处仅列易漏的两项：
pyarrow==24.0.0
pyworld==0.3.5
# SenseVoice 量化 onnx 版语音识别（更小更快，CPU）。注：装它会把 numpy 锁到 1.26.4（cv2/ultralytics 仍兼容）。
funasr_onnx==0.4.1
# VibeVoice-Realtime-0.5B 文本转语音（预置音色，CPU）。官方推理代码非 pip 包：
#   git clone https://github.com/microsoft/VibeVoice 到 third_party/VibeVoice（含 demo/voices 预置音色）。
#   运行时 inference._ensure_vibevoice_path 注入 sys.path。装 vibevoice 会带来 transformers 4.51/accelerate/gradio 等。
accelerate==1.6.0
# MeloTTS 中英混合文本转语音：sherpa-onnx 纯 onnx 引擎（CPU，win wheel）。
sherpa-onnx==1.13.3
# DeepSeek AI 报告：HTTP 调用 + 纯逻辑单测
requests==2.32.3
pytest==8.3.3
# VoxCPM2 文本转语音 / 音色克隆 / 音色设计（openbmb/VoxCPM2，HF voxcpm 库，48kHz 输出）
# 注意：安装 voxcpm 会把 modelscope 升到 ~1.37、datasets 升到 ~3.6（torch 2.11.0 不受影响，已验证 import app/funasr/modelscope 正常）。
voxcpm==2.0.3
