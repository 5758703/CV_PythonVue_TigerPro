Flask==3.0.3
Flask-Cors==4.0.1
Flask-SQLAlchemy==3.1.1
Flask-JWT-Extended==4.6.0
PyMySQL==1.1.1
cryptography>=44.0.0,<50.0.0
python-dotenv==1.0.1
ultralytics==8.4.69
# transformers 5.x 要求 huggingface-hub>=1.5,<2.0（与旧版 0.36 不兼容）
huggingface_hub>=1.5.0,<2.0.0
# transformers 5.x：RF-DETR(rfdetr) 需要 BackboneConfigMixin；NLP/TTS/Whisper 等经回归仍可用。
transformers>=5.1.0,<6.0.0
# RF-DETR 实时目标检测（Roboflow/rf-detr-medium 等，COCO 80 类）
rfdetr>=1.8.0
# MobileSAM 交互式分割（CPU 可用，vit_t 约 40MB；import 名为 mobile_sam）
# 官方源：https://github.com/ChaoningZhang/MobileSAM
# 国内镜像（AtomGit/GitCode，内容一致，便于国内 pip 安装）：
mobile-sam @ git+https://gitcode.com/ChaoningZhang/MobileSAM.git
# 若可直连 GitHub，可改回：
# mobile-sam @ git+https://github.com/ChaoningZhang/MobileSAM.git
# RTMO 姿态估计（rtmlib + onnxruntime，羽毛球分析等）
rtmlib>=0.0.15
onnxruntime>=1.17.0
timm==1.0.27
sentencepiece==0.2.1
imageio==2.37.3
imageio-ffmpeg==0.6.0
# modelscope 1.37.1：voxcpm 依赖会升到此版本，已验证 snapshot_download/HubApi 与 funasr 仍正常。
modelscope==1.37.1
# 语音识别(SenseVoice)：funasr 框架 + torchaudio 加载音频。
# 注意 torch 全家桶须对齐：torch/torchaudio 2.11.0、torchvision 0.26.0（CPU 版，pytorch cpu 源）。
torch==2.11.0
torchvision==0.26.0
torchaudio==2.11.0
funasr==1.3.10
# SenseVoice 量化 onnx 版语音识别（更小更快，CPU）。注：装它会把 numpy 锁到 1.26.4（cv2/ultralytics 仍兼容）。
funasr_onnx==0.4.1
# VibeVoice-Realtime-0.5B 文本转语音（预置音色，CPU）。官方推理代码非 pip 包：
#   git clone https://github.com/microsoft/VibeVoice 到 backend/uploads/models/third_party/VibeVoice（含 demo/voices 预置音色）。
#   运行时 inference._ensure_vibevoice_path 注入 sys.path。装 vibevoice 会带来 transformers 4.51/accelerate/gradio 等。
accelerate==1.6.0
# MeloTTS 中英混合文本转语音：sherpa-onnx 纯 onnx 引擎（CPU，win wheel）。
sherpa-onnx==1.13.3
# Roboflow Universe 模型权重拉取（如 NASASpaceflight Rocket Detect）
roboflow==1.3.13
# DeepSeek AI 报告：HTTP 调用 + 纯逻辑单测
requests==2.32.3
pytest==8.3.3
# VoxCPM2 文本转语音 / 音色克隆 / 音色设计（openbmb/VoxCPM2，HF voxcpm 库，48kHz 输出）
# 注意：安装 voxcpm 会把 modelscope 升到 ~1.37、datasets 升到 ~3.6（torch 2.11.0 不受影响，已验证 import app/funasr/modelscope 正常）。
voxcpm==2.0.3
# PaddleOCR onnx 文字识别（RapidOCR 封装 PP-OCR det+rec，CPU）
rapidocr_onnxruntime==1.4.4
# InsightFace 人脸识别（SCRFD + ArcFace ONNX；CPU 用 onnxruntime，GPU 可另装 onnxruntime-gpu）
insightface>=0.7.3
